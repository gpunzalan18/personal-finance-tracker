{
  "name": "budget-plan",
  "version": "1.0.0",
  "description": "automate budget planning",
  "main": "index.js",
  "scripts": {
    "service": "nodemon --watch 'src/**/*.ts' --ignore 'src/**/*.spec.ts' --exec ts-node src/app.ts",
    "ui": "cd ui && ng serve",
    "start": "cp -r ./assets ./dist/assets && cd ui && ng build && cd ../ && tsc && node dist/app.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/express": "^4.16.1",
    "nodemon": "^2.0.7",
    "tslint": "^5.12.1",
    "typescript": "^3.3.3"
  },
  "dependencies": {
    "ejs": "^3.1.5",
    "express": "^4.16.4",
    "lodash.filter": "^4.6.0",
    "pdf-parse": "^1.1.1",
    "pdf2json": "^1.2.0",
    "ts-node": "^9.1.1"
  }
}
