{
  "name": "backend",
  "version": "1.0.0",
  "description": "automate budget planning",
  "main": "index.js",
  "scripts": {
    "start:backend": "nodemon --watch 'src/**/*.ts' --ignore 'src/**/*.spec.ts' --exec ts-node src/app.ts",
    "start:frontend": "cd ../frontend && ng serve",
    "start": "cp -r ./backend/assets ./backend/dist/assets && cd ./frontend && ng build  && cd ../../ && tsc && node dist/app.js",
    "test": "cp -r ./backend/assets ./backend/dist/assets "
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {},
  "dependencies": {}
}
